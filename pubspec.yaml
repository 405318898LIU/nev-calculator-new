name: nev_investment_calculator
description: 新能源投资收益计算器 - 基于机制电价政策
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.66.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  share_plus: ^7.2.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/data/
